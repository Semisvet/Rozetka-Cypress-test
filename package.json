{
  "name": "rozetka-cypress-test",
  "version": "1.0.0",
  "scripts": {
    "develop": "npx cypress open",
    "headless": "npx cypress run --env allure=true --browser chrome",
    "open-report": "allure serve ./allure-results"
  },
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.37.0",
    "cypress": "^12.9.0"
  }
}
